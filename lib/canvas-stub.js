/**
 * Stub for the native 'canvas' npm package.
 * Konva's Node.js entry (index-node.js) requires 'canvas' for server-side rendering.
 * Since we only render Konva client-side, this stub provides no-op exports
 * to prevent build errors during Next.js SSR.
 */
module.exports = {};
module.exports.default = {};
